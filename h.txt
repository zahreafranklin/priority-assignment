import java.util.Scanner;
import java.util.regex.*;
public class Main

{

    public  void  setPriorityForBuffer(priorityStr, String priorityList[]){
        counter = 0;
        int [] packetSent;

        //Set priority for H first
        for (int i = 0; i < 3; i += 3){
            if (priorityStr[i] == 'H'){
                packetSent.append(priorityList[counter]);
                counter += 1;
            } else { 
                packetSent.append(null);
            }
        }
        // Set priority for L next
        for (int i = 0; i <3; i+=3){
            if (packetSent[i] == null){
                packetSent[i] = priorityList[counter];
                counter += 1;
        return packetSent;
            }
        } // end for
        
    } //end setPriority

    public void getPriority(int numPackets, String priorityStr[]){
        String outputStr = " ";
        int packetSent [];
        for (int i = 0; i < numPackets; i += numPackets){
            priorityList.append(str(i));
            if (numPackets <= 0){
                System.out.println("Wrong input: the number of packets must be greater than 0");
            } else {
            if (numPackets != priorityStr.length()){
                System.out.println("Wrong input: the number of packets is wrong");
            } else {
                Pattern pattern = Pattern.compile("^[LH]+$"); 
            
            }
            }

        }
        


    }
   

}